name: arjolle
description: "Application de gestion de présence Arjolle"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.7.2 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  intl: ^0.20.2
  shared_preferences: ^2.5.3
  path_provider: ^2.1.5
  http: ^0.13.5
  connectivity_plus: ^3.0.2
  flutter_local_notifications: ^18.0.1
  timezone: ^0.9.4
  flutter_riverpod: ^2.4.0
  provider: ^6.1.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
